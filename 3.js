// veuillez complétez la fonction, ainsi que son argument
function nombrePair(x) {
    var valiny=true;
    if (x%2==0) 
    {
      console.log("true  (le nombre est paire)");  
      valiny=true
      return true;
        
    }
      console.log("false (le nombre est impaire)");
      valiny=false;
      return false;
}
nombrePair(16);
